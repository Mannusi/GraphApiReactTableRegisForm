import * as React from 'react';
import {ITestDropdownProps} from './ITestDropdownProps';

export default class ReactLifecycle extends React.PureComponent<ITestDropdownProps,any>{
    constructor(props) {
        super(props)
      
        this.state = {
           
        }
      }
}
